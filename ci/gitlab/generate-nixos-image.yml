generate-nixos-image:
  stage: setup
  tags:
    - grid5000-shell
  script:
    - |
      rsync -av ci/gitlab/generate-nixos-image_deploy-batch.bash ajenkins@luxembourg:
      ssh ajenkins@luxembourg < ci/gitlab/generate-nixos-image_launcher.bash
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
