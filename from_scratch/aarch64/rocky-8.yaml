#==============================================================================
# vim: softtabstop=2 shiftwidth=2 expandtab fenc=utf-8 cc=81 tw=80
#==============================================================================
#
# DESCRIPTION: Rocky Linux 8 ARM64 generic recipe using the netinstall mechanism
#
# Uses the netinstall + kickstart mechanism
#
#==============================================================================
---
extend: rocky-base.yaml
global:
  distrib: rocky

  release_number: 8
  release: 8

bootstrap:
  - "@base"

setup:
  - "@base"

export:
  - "@base"
